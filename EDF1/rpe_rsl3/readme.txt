After running `preprocess.ijm` to process the raw images
1. run `export_fig.ijm` to get the images in the extended data figure 1f (need to change path)
2. run `outline_s3.m` to get outline in the extended data figure 1f
3. run `entropy_s3.m` to get the vector field and entropy (entropy_s3.mat)
