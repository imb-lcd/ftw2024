1. run `preprocess.ijm` to process the raw images (need to change path)
2. run `export.ijm' to get the images at the top panel of figure 2a (need to change path)
3. run `plot_profile.m` to get the bottom panel of figure 2a.
4. run `passing_prob.m` to get figure 2b.