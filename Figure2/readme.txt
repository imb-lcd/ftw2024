After running `preprocess.ijm` to process the raw images,
1. run `export.ijm' to get the images at the top panel of figure 2a
3. run `plot_profile.m` to get the bottom panel of figure 2a.
4. run `passing_prob.m` to get figure 2b.